import styled from '@emotion/styled'

export const Wrapper = styled.main`
  min-height: 100%;
  padding: 2.5rem 0;
  margin: 0 auto;
  background: var(--N10);
  display: flex;
  flex-direction: column;
  gap: 4rem;
`
