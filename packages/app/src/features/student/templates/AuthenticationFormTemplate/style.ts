import styled from '@emotion/styled'

export const Wrapper = styled.div`
  width: 100%;
  min-height: 100%;
  padding: 2.5rem 0;
  display: flex;
  flex-direction: column;
  gap: 3rem;
  background: var(--N10);
`
